<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="us_messenger_operator_view_tree" model="ir.ui.view">
        <field name="name">us.messenger.operator.list</field>
        <field name="model">us.messenger.operator</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="user_id"/>
                <field name="bot_id"/>
                <field name="nickname"/>
                <field name="priority"/>
                <field name="channel_count"/>
                <field name="messenger_type"/>
            </list>
        </field>
    </record>

    <record id="us_messenger_operator_view_form" model="ir.ui.view">
        <field name="name">us.messenger.operator.form</field>
        <field name="model">us.messenger.operator</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="user_id"/>
                        <field name="bot_id"/>
                        <field name="nickname"/>
                        <field name="priority"/>
                        <field name="channel_count"/>
                        <field name="messenger_type"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="messenger_operator_action" model="ir.actions.act_window">
        <field name="name">Operators</field>
        <field name="res_model">us.messenger.operator</field>
        <field name="view_mode">kanban</field>
    </record>

    <record id="view_us_messenger_operator_kanban" model="ir.ui.view">
        <field name="name">us.messenger.operator.kanban</field>
        <field name="model">us.messenger.operator</field>
        <field name="arch" type="xml">
            <kanban default_group_by="bot_id"
                    sample="1"
                    quick_create="false"
                    group_create="false" 
                    group_delete="false" 
                    group_edit="false"
                    records_draggable="false"
                    type="object" 
                    action="action_operator_channels">
                
                <field name="user_id"/>
                <field name="nickname"/>
                <field name="bot_id"/>
                <field name="channel_count"/>
                <field name="messenger_type"/>
                <field name="avatar_128"/>
                <field name="priority"/>

                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click o_kanban_record d-flex flex-column p-3">
                            <div class="o_kanban_record_top d-flex align-items-start mb-2">
                                <div class="o_kanban_image_fill_left d-none d-md-block me-3">
                                    <field name="avatar_128" widget="image" class="o_kanban_image_inner_pic" options="{'size': [60, 60]}"/>
                                </div>
                                
                                <div class="o_kanban_record_headings w-100">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <strong class="o_kanban_record_title fs-5 text-primary">
                                            <t t-if="record.nickname.raw_value">
                                                <field name="nickname"/>
                                            </t>
                                            <t t-else="">
                                                <field name="user_id"/>
                                            </t>
                                        </strong>
                                        <div class="text-muted small">
                                            <i class="fa fa-star me-1" title="Bot"/> 
                                            <strong>Priority:</strong> <field name="priority"/>
                                        </div>
                                    </div>
                                    
                                    <div class="mt-1">
                                        <field name="messenger_type" 
                                            widget="badge" 
                                            decoration-info="messenger_type == 'telegram'"
                                            decoration-secondary="messenger_type == 'viber'"
                                            decoration-success="messenger_type == 'whatsapp_twilio'"
                                            decoration-danger="messenger_type == 'instagram'"/>
                                    </div>
                                </div>
                            </div>

                            <div class="o_kanban_record_bottom d-flex justify-content-between align-items-center mt-2 pt-2 border-top">
                                <div class="oe_kanban_bottom_left">
                                    <span class="badge rounded-pill bg-100 text-dark border">
                                        <i class="fa fa-comments me-1 text-primary"/>
                                        <field name="channel_count"/> channels
                                    </span>
                                </div>
                                <div class="oe_kanban_bottom_right">
                                    <field name="user_id" widget="many2one_avatar_user"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="discuss_channel_view_list" model="ir.ui.view">
        <field name="name">discuss.channel.list</field>
        <field name="model">us.messenger.partner</field>
        <field name="arch" type="xml">
            <list js_class="us_messenger.discuss_channel_list" string="History" delete="false" create="false" default_order="create_date desc">
                <field name="channel_id" column_invisible="True"/>
                <field name="messenger_operator_id" column_invisible="True"/>
                <field name="create_date" string="Session Date"/>
                <field name="name" string="Participants"/>
                <field name="message_count" string="# Messages"/>
                <button name="action_change_operator" type="object" class="oe_highlight" groups="us_messenger.us_messenger_group_user"
                        string="CHANGE OPERATOR" invisible="is_hide or not messenger_operator_id"/>
                <button name="action_change_operator" type="object" class="oe_highlight" groups="us_messenger.us_messenger_group_manager"
                        string="CHANGE OPERATOR" invisible="not(is_hide and messenger_operator_id)"/>
            </list>
        </field>
    </record>
</odoo>